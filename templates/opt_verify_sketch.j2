// This is an autogenerated sketch snippet corresponding to the switch's data
// path and is used to solve the optimization correctness verification problem.

{# Include sub-template to contain definitions required to write the sketch #}
{% include 'templates/sketch_defs.j2' }

|StateAndPacket| pipeline (|StateAndPacket| state_and_packet, {{ hole_args }})
  implements program {
  {# Include sub-template for the main sketch body #}
  {% include 'templates/sketch_body.j2' %}
}
